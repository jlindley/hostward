# TODO

- Notifications: `doctor` now checks adapter availability, but macOS notification permission/style diagnostics are still shallow because the current adapter is just `osascript`.
- Script runner UX: refine timeout, output truncation, working directory, and environment inheritance defaults once the runner package is in place.
